/**
 * src/app/sys-simulation/[id]/page.tsx
 *
 * CSR builder + simulation page.
 *
 * WHY THIS EXISTS:
 * Each challenge opens an interactive builder where users design architectures
 * and run simulations. Client-rendered for rich interactivity (canvas, state, timers).
 */

export default function ChallengeBuilderPage() {
  return null
}

/**
 * src/components/ui/Button.tsx
 *
 * Shared button component with variants.
 *
 * WHY THIS EXISTS:
 * Primary actions (start simulation, retry, navigate) share the same interactive
 * styling. Centralizing button variants avoids duplicated Tailwind classes.
 */

export default function Button() {
  return null
}

/**
 * src/components/ui/StatCard.tsx
 *
 * Displays a single metric (label + value).
 *
 * WHY THIS EXISTS:
 * The builder and report views show live metrics (RPS, latency, budget).
 * StatCard provides a consistent layout for each metric tile.
 */

export default function StatCard() {
  return null
}

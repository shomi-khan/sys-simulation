/**
 * src/components/ui/Badge.tsx
 *
 * Shared badge component for difficulty labels.
 *
 * WHY THIS EXISTS:
 * Challenges display difficulty levels (beginner, medium, etc.) as colored badges.
 * A single reusable Badge keeps styling consistent across the challenge list and builder.
 */

export default function Badge() {
  return null
}

/**
 * src/components/ui/Terminal.tsx
 *
 * Scrolling log terminal UI.
 *
 * WHY THIS EXISTS:
 * During simulation, the engine emits log entries (warnings, errors, system events).
 * Terminal renders these in a monospace, auto-scrolling panel for real-time feedback.
 */

export default function Terminal() {
  return null
}

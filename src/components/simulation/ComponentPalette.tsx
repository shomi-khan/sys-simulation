/**
 * src/components/simulation/ComponentPalette.tsx
 *
 * Sidebar with draggable components.
 *
 * WHY THIS EXISTS:
 * Each challenge exposes a subset of infrastructure components from the registry.
 * The palette lists available components for drag-and-drop onto the canvas.
 */

export default function ComponentPalette() {
  return null
}

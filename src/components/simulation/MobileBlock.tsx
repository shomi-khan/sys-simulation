/**
 * src/components/simulation/MobileBlock.tsx
 *
 * Full-screen mobile warning.
 *
 * WHY THIS EXISTS:
 * The canvas builder requires a minimum viewport width for usable drag-and-drop.
 * MobileBlock replaces the builder on small screens with a friendly message.
 */

export default function MobileBlock() {
  return null
}

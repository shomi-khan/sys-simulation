/**
 * src/components/simulation/Canvas.tsx
 *
 * React Flow drag-and-drop canvas.
 *
 * WHY THIS EXISTS:
 * Users build architectures by placing infrastructure nodes and connecting them with edges.
 * React Flow provides pan/zoom, node dragging, and edge drawing on an infinite canvas.
 */

export default function Canvas() {
  return null
}

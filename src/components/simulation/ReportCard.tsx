/**
 * src/components/simulation/ReportCard.tsx
 *
 * Post-simulation result display.
 *
 * WHY THIS EXISTS:
 * After a simulation completes, users see aggregated metrics, pass/fail status,
 * and score breakdown. ReportCard presents the SimulationResult in a readable format.
 */

export default function ReportCard() {
  return null
}
